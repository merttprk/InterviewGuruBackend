# Backend CI — Functions derlemesi ve testler (proje interviewguru/ alt klasöründe). Deploy YAPILMAZ (secret'lar ve onay kullanıcıda).
name: Functions

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: functions-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: interviewguru/functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: interviewguru/functions/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: npm test
      - name: İçerik doğrulama (dersler/keşfet JSON)
        working-directory: interviewguru/scripts/content-build
        run: node validate.js
